{
  "main": "server.js",
  "engines": {
    "npm": "^8.5.5",
    "node": "^16.15.0"
  },
  "scripts": {
    "postinstall": "concurrently \"cd ./app && npm install && npm run build\" \"cd ../server && npm install\" ",
    "start": "cd ./server && npm start"
  },
  "dependencies": {
    "concurrently": "^7.4.0"
  }
}
